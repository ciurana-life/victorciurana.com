from _typeshed import NoneType


def send_verification_email(email: str) -> bool:
    # [1] Check if we have alrready the email
    # TRY else return False

    # [2] model.save created the url token,
    #    call something like model.send_email  (celery)

    #  Return True

